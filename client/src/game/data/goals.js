// Straight re-export of the shared goal/repeatable defs - the reducer
// (claimGoal/claimRepeatable) and GoalsPanel must see identical progress
// formulas, targets, and rewards.
export { GOAL_DEFS, REPEATABLE_DEFS } from '@shared/goals.js';
